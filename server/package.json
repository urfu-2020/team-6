{
  "name": "team-6",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.17.1",
    "path": "^0.12.7"
  },
  "devDependencies": {},
  "engines": {
    "node": "14.12.0"
  },
  "scripts": {
    "start": "node index.js",
    "build": "cd ../client && npm install && npm run build"
  }
}
